export interface FoodItem {
  name: string;
  emoji: string;
  categories: string[];
  tags: string[];
}

export const foodItems: FoodItem[] = [
  // ... (keep existing 94 items, but I will replace the whole file for safety)
  // I will prepare a giant list
];
